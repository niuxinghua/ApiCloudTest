
20160325更新：

1、APIManager增加 - (void)initSDKWithLaunchOptions:(NSDictionary *)launchOptions方法，同时废弃 - (void)initSDK方法。
2、增加APIUpdateManager类，管理更新操作。
3、增加云修复完成事件，处理重启应用。


20160428更新：

1、为更好的处理第三方框架冲突问题，现将APICloud.a里面用到的第三方框架拆分了出来，得到了libminizip.a、libopencore-amrnb.a、libopencore-amrwb.a，放在了Modules目录下面。

2、UZWebView由之前的继承于UIWebView变更为继承于UIView，同时增加UZWebViewProtocol协议。


20160720更新：

1、APIWidgetContainer增加初始化方法 + (instancetype)widgetContainerWithAttribute:(NSDictionary *)attribute，可以对页面进行更多配置
2、APIWindowContainer增加初始化方法 + (instancetype)windowContainerWithAttribute:(NSDictionary *)attribute，可以对页面进行更多配置